# Specification

## Summary
**Goal:** Update the logo text color to solid blue in the site header and footer.

**Planned changes:**
- Change the logo text in `SiteHeader.tsx` to render in a full, solid blue color using existing blue color tokens
- Apply the same full blue color to the logo text in `SiteFooter.tsx` if present

**User-visible outcome:** The site logo text appears in a consistent solid blue color in both the header and footer.
